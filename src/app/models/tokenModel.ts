export interface TokenModel{
    token:string;
    expiration:string;

}


// bu burada dursun 
/* export interface LoginResponseModel {
  data: {
    claims: string[]; // Kullanıcının yetkileri veya rollerini içeren diziler
    token: string; // JWT token
    expiration: string; // Token geçerlilik süresi
    refreshToken: string; // Token yenileme anahtarı
  };
  success: boolean; // İşlem başarılı mı
  message: string; // Geri bildirim mesajı (örneğin: "Login successful")
}
*/
